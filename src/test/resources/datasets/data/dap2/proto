message Dap2 {
    message Samples {
        repeated string myTime = 1;
        repeated int64 myInt = 2;
        repeated float myReal = 3;
        repeated string myText = 4;
    }
    optional Samples samples = 1;
}