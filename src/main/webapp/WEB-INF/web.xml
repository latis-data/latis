<?xml version="1.0" encoding="UTF-8"?>
<web-app>

    <servlet>
        <servlet-name>LatisServer</servlet-name>
        <servlet-class>latis.server.LatisServer</servlet-class>
        <!--  init-param>
            <param-name>config</param-name>
            <param-value>resources/latis.properties</param-value>
        </init-param-->
        <load-on-startup>1</load-on-startup>
    </servlet>

    <!--servlet>
      <servlet-name>fileServlet</servlet-name>
      <servlet-class>net.balusc.webapp.FileServlet</servlet-class>
      <init-param>
        <param-name>basePath</param-name>
        <param-value>/</param-value>
      </init-param>
    </servlet-->
    
    <servlet-mapping>
        <servlet-name>LatisServer</servlet-name>
        <url-pattern>/dap/*</url-pattern>
    </servlet-mapping>

    <!-- The mapping for the default servlet -->
    <!--servlet-mapping>
        <servlet-name>fileServlet</servlet-name>
        <url-pattern>/resources/*</url-pattern>
    </servlet-mapping-->

<resource-ref>
  <description>
    Database resource for MAVEN Key Parameters
  </description>
  <res-ref-name>
    jdbc/maven
  </res-ref-name>
  <res-type>
    javax.sql.DataSource
  </res-type>
  <res-auth>
    Container
  </res-auth>
</resource-ref>

<resource-ref>
  <description>
    Database resource for MMS SITL
  </description>
  <res-ref-name>
    jdbc/mms
  </res-ref-name>
  <res-type>
    javax.sql.DataSource
  </res-type>
  <res-auth>
    Container
  </res-auth>
</resource-ref>

</web-app>

